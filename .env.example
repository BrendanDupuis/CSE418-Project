# MongoDB Configuration
# Required for storing encrypted messages, user public keys, and 2FA verification codes
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=cse418_project

# Resend Email Service Configuration for 2FA
# Sign up at https://resend.com to get your API key
RESEND_API_KEY=your-resend-api-key-here
RESEND_FROM_EMAIL=onboarding@resend.dev

# Never commit the actual .env.local file with real credentials
# MongoDB Atlas: mongodb+srv://<username>:<password>@cluster.mongodb.net/
# Local MongoDB: mongodb://localhost:27017
# For Resend: Get your API key from https://resend.com/api-keys
# RESEND_FROM_EMAIL must be a verified domain in your Resend account
# If RESEND_API_KEY is not set, codes will be logged to console for development
